import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '95%',
        alignSelf: 'center',
        marginTop: '10%'
    },
    header: {
        color: 'white',
        fontSize: 24,
        fontWeight: '600'
    }
})

export default styles