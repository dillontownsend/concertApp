import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    text: {
        color: 'white',
        alignSelf: 'center',
        textAlign: 'center',
        fontSize: 18,
        marginTop: '15%',
        width: '95%',
    }
})

export default styles