import { StyleSheet } from 'react-native'
import globalColors from '../../../global/globalColors'

const styles = StyleSheet.create({
	divider: {
		borderBottomColor: globalColors.gray600,
		borderBottomWidth: 0.5,
		width: '98%',
		alignSelf: 'center',
	},
})

export default styles
